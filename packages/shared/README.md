# @yuru-agent/shared

Reserved package for API contract types and shared validation helpers.

The first project foundation phase keeps this package empty on purpose. Shared
types should be added only when both the FastAPI backend and Next.js frontend
need the same stable contract.
